console.log('Hello TypeScript');

let num: number = 3;

console.log(num);

//! cf) ts 코드는 실시간으로 js컴파일 반영 x
// +) TS 파일명은 node 파일명.ts로 실행되지 않음

// ts-node를 사용한 실시간 번역 및 실행
// :js 파일 생성dksgka
// : npm install ts-node --save-dev
// : npm install -g ts-node
//? 설치 후 반드시 package.json파일 확인
// 버전 확인 npx ts-node -v
